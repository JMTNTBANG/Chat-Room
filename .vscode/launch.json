{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Deploy",
            "request": "launch",
            "command": "npm run deploy",
            "cwd": "${workspaceFolder}"
        }
    ]
}